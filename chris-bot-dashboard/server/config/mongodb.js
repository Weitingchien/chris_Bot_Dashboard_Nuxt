//import mongoose from 'mongoose';
//import { Nitro } from 'nitropack';
/*
const config = useRuntimeConfig();

export default async () => {
  const ytChannelDB = await mongoose.createConnection(
    config.MONGODBCONNECTIONSTRINGYTCHANNEL,
    { useNewUrlParser: true, useUnifiedTopology: true }
  );

  const chrisbotDB = await mongoose.createConnection(
    config.MONGODBCONNECTIONSTRINGCHRISBOTDB,
    { useNewUrlParser: true, useUnifiedTopology: true }
  );
}
*/

//return ytChannelDB;
//console.log(ytChannelDB);
/*
    .then(() => {
      console.log('Connected to MongoDB!');
    })
    .catch(err => {
      console.warn(`Error occurred: ${err}`);
    });
    */
//return ytChannelDB;

/*
async function apexChannelDB(mainConnection) {
  const apexChannelDBConnection = mainConnection.useDb('ytChannel');
  console.log(apexChannelDBConnection);
}
*/
